version: "3.8"

services:
  kiosk-llm:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: kiosk-llm-dev
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - ENV=development
      - LOG_LEVEL=DEBUG
    devices:
      # Camera device (comment out if not available)
      - /dev/video0:/dev/video0
    stdin_open: true
    tty: true
    restart: unless-stopped

  # Database service for development
  sqlite-db:
    image: alpine:latest
    container_name: kiosk-sqlite
    volumes:
      - ./data:/data
    command:
      [
        "sh",
        "-c",
        "apk add --no-cache sqlite && sqlite3 /data/kiosk_llm.db 'SELECT 1;' && while true; do sleep 30; done",
      ]
    environment:
      - SQLITE_DB_PATH=/data/kiosk_llm.db

networks:
  default:
    name: kiosk-network
